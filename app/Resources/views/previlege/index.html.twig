{% extends 'base.html.twig' %}

{% block body %}
    <h1></h1>
    <div class="container-fluid">
    <div class="row" style="height:20px;"> 
    </div>
    <div class="row"> 
        <div class="col-md-1"> 
        </div>
        <div class="col-md-10"> 
    <div class="box box-info">
        <div class="box-header with-border">
             <h3 class="box-title">previlege List</h3>
             <button type="submit" class="btn btn-info pull-right"><a href="{{ path('previlege_new') }}">Create a new entry</a></button>
        </div><!-- /.box-header -->
         <!-- form start -->
        <form class="form-horizontal">
        <div class="box-body">
            <table class="table">
                <thead>
                    <tr>
                        <th></th>
                        <th>Previlege</th>
                        <th>Description</th>
                    </tr>
                </thead>
                <tbody>
                {% for previlege in previleges %}
                    <tr>
                        <td>{{ previlege['id'] }}</td>
                        <td>{{ previlege['previlege'] }}</td>
                        <td>{{ previlege['description'] }}</td>
                        
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('previlege_show', { 'id': previlege['id'] }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('previlege_edit', { 'id': previlege['id'] }) }}">edit</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div><!-- /.box-body -->
        <div class="box-footer">
        </div><!-- /.box-footer -->
        </form>
    </div>
    </div>
    </div>
    </div>
{% endblock %}
